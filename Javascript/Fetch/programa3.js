

// vamos a ahcer el esqueleto basico de la api fetch 

const url = 'url de ejemplo '

fetch(url)
	.then(response => {
		// en este then se manejea si se conecto con la api 
	})
	.then(data => {
		// en este then manejamos los datos que obtenemos 
	})
	.catch(error => {
		// aqui gestionamos cualquier error que pueda pasar
	})
