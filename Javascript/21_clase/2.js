/**
 * 2. Encontrar el máximo:
Escribe una función que encuentre el elemento máximo en un array de números.
 */
 function funcion(){
    // creamos el arreglo 
    let arreglo = [12,46,8,4,2,765,23,90,1]
    let numero = 0 
    for (let i = 0 ; i < arreglo.length ; i++){
        if (numero < arreglo[i]){
            numero = arreglo[i]
        }else {

        }
    }
    console.log("El numero maximo es : " , numero)
 }

 funcion()